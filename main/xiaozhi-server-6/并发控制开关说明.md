# 🔧 并发控制开关说明

## ✅ 当前状态：已关闭

### 📍 修改位置

1. **core/connection.py 第731行**
   ```python
   # 关闭状态：
   llm_pool = get_llm_pool(max_concurrent=999)  # 设置超大值 = 无限制
   
   # 恢复并发控制：
   # llm_pool = get_llm_pool(max_concurrent=3)  # 最大3个并发
   ```

2. **core/providers/intent/intent_llm/intent_llm.py 第159行和第262行**
   ```python
   # 关闭状态：
   llm_pool = get_llm_pool(max_concurrent=999)
   
   # 恢复并发控制：
   # llm_pool = get_llm_pool(max_concurrent=3)
   ```

3. **core/utils/llm_pool.py 第92行**
   ```python
   # 关闭状态：
   self.rate_limiter = LLMRateLimiter(max_calls_per_minute=99999, max_calls_per_second=99999)  # 关闭频率限制
   
   # 恢复频率控制：
   # self.rate_limiter = LLMRateLimiter()  # 默认每分钟60次、每秒3次
   ```

## 🔄 快速切换方法

### 关闭所有限制（当前状态）
- 并发数：999（无限制）
- 频率限制：99999/秒（无限制）

### 恢复默认限制
- 并发数：3（推荐值）
- 频率限制：60次/分钟，3次/秒

### 自定义配置
```python
# 轻度限制
llm_pool = get_llm_pool(max_concurrent=10)

# 中度限制（推荐）  
llm_pool = get_llm_pool(max_concurrent=3)

# 严格限制
llm_pool = get_llm_pool(max_concurrent=1)
```

## ⚠️ 注意事项

- 关闭限制可能导致API调用过快被限流
- 建议在测试环境先验证效果
- 保留错误处理和性能监控功能不受影响
